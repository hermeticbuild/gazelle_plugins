load("@aspect_rules_ts//ts:defs.bzl", "ts_project")

ts_project(
    name = "core",
    srcs = ["types.ts"],
    composite = True,
    declaration = True,
    declaration_map = True,
    source_map = True,
    tsconfig = "//:tsconfig",
    visibility = ["//visibility:public"],
)
