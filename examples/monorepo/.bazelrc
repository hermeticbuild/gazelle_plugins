# Mirrors the host repo's .bazelrc — needed so rules_rs's experimental
# Linux toolchains pick up the gnu.2.28 libc constraint.
common --enable_platform_specific_config
common:linux --host_platform=@gazelle_plugins//:local_gnu_platform

build --show_result=0
test --test_output=errors
