name: ci

on:
  push:
    branches: [main]
  pull_request:
  merge_group:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  # Emits the platform matrix once. macOS is dropped on pull_request runs
  # (saves runner minutes) but stays for push to main and the merge queue.
  matrix:
    name: matrix
    runs-on: ubuntu-latest
    outputs:
      platforms: ${{ steps.set.outputs.platforms }}
    steps:
      - id: set
        env:
          EVENT: ${{ github.event_name }}
        run: |
          if [ "$EVENT" = "pull_request" ]; then
            echo 'platforms=[{"platform":"linux-x86_64","runner":"ubuntu-latest"}]' >> "$GITHUB_OUTPUT"
          else
            echo 'platforms=[{"platform":"linux-x86_64","runner":"ubuntu-latest"},{"platform":"macos-arm64","runner":"macos-latest"}]' >> "$GITHUB_OUTPUT"
          fi

  bazel-test:
    needs: matrix
    name: bazel ${{ matrix.bazel }} test (${{ matrix.os.platform }})
    runs-on: ${{ matrix.os.runner }}
    strategy:
      fail-fast: false
      matrix:
        os: ${{ fromJSON(needs.matrix.outputs.platforms) }}
        bazel: ["8.5.1", "9.0.0"]
    env:
      USE_BAZEL_VERSION: ${{ matrix.bazel }}

    steps:
      - uses: actions/checkout@v6

      - name: Setup Bazel
        uses: bazel-contrib/setup-bazel@0.19.0
        with:
          bazelisk-cache: true
          disk-cache: ${{ github.workflow }}-${{ matrix.os.platform }}-bazel${{ matrix.bazel }}
          repository-cache: true

      - name: bazel test //...
        run: bazelisk test //...

      # BCR's anonymous_module_runner builds gazelle_ts as a dep, so our
      # workspace .bazelrc never applies. Re-build //ts/... with
      # --noworkspace_rc to catch any flag we'd silently rely on (e.g. a
      # --host_platform pin that downstream consumers wouldn't have).
      - name: bazel build //ts/... (--noworkspace_rc, mimics BCR consumer)
        run: bazelisk --noworkspace_rc build //ts/...

  # Examples are linux-only — they exercise the BUILD-generation path and
  # don't add platform-specific coverage worth the macOS runner cost.
  examples:
    name: example ${{ matrix.example }} (bazel ${{ matrix.bazel }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        example: [basic, composite, graphql, advanced]
        bazel: ["8.5.1", "9.0.0"]
    env:
      USE_BAZEL_VERSION: ${{ matrix.bazel }}

    steps:
      - uses: actions/checkout@v6

      - name: Setup Bazel
        uses: bazel-contrib/setup-bazel@0.19.0
        with:
          bazelisk-cache: true
          disk-cache: ${{ github.workflow }}-examples-${{ matrix.example }}-bazel${{ matrix.bazel }}
          repository-cache: true

      - name: bazel test //... (examples/${{ matrix.example }})
        working-directory: examples/${{ matrix.example }}
        run: bazelisk test //...

      - name: gazelle idempotency check (examples/${{ matrix.example }})
        working-directory: examples/${{ matrix.example }}
        run: |
          bazelisk run //:gazelle -- update -mode=diff
